package com.acadgild.widgetexamplesession12;

/**
 * Created by AdityaDua on 28/06/17.
 */

public class Notes {
    /**
     * Today's : Widgets :: ShortCuts...
     * Active : you see the application Up & Down...
     * Components : Service : is has no UI but it remains active even if the
     * activity which called the service dies away.
     * Music Player : select : activity &
     *
     * Which component was that ? SERVICE
     *
     * Which component ? listener , it listens till we wake it up.
     * Broadcast Receiver : Rebooted
     * Battery Low : That Level again...: phone is connecte dto a charger or rebooted
     * you get a call
     *
     *
     */
}
